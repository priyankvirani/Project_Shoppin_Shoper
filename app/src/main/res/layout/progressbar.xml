<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rlvGlobalProgressbar"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="@android:color/transparent"
    android:clickable="true"
    android:gravity="center"
    android:visibility="gone">

    <!--    <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:backgroundTint="@color/app_theme_1"
            android:backgroundTintMode="multiply"
            android:indeterminate="true"
            android:indeterminateDrawable="@drawable/circle_pro"
            android:visibility="visible" />-->

    <!--    <com.shoppin.customer.viewcomponent.ProgressWheel
            android:layout_width="@dimen/drawable_progressbar"
            android:layout_height="@dimen/drawable_progressbar"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:background="@drawable/circle_shadow"
            wheel:barColor="@color/app_theme_1"
            wheel:progressIndeterminate="true" />-->

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/circle_pro"
        android:padding="@dimen/margin_tiny"
        android:visibility="visible" />

    <!--android:background="@drawable/circle_shadow"-->

</RelativeLayout>